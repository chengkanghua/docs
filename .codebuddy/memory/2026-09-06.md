# 2026-09-06

- 精简仓库根 `.gitignore`: 项目实际仅 Python + mkdocs, 去掉原 183 行中 Django/Flask/Scrapy/SageMath/PyInstaller/Celery/Rope/Spyder/PyBuilder/Jupyter/IPython/pyenv/pipenv/PEP582/Sphinx/Pyre/Translations 等框架特定无用条目; 仅保留 site/、.cache/、Python 虚拟环境、缓存/编译产物、打包、测试/lint 缓存、日志、.env/IDE/系统文件(.DS_Store 等)。
- 排查 docsify 残留文件(不影响 mkdocs 构建, 由用户自行决定是否删除):
  - `_sidebar.md` ×8: auto、k8s-2023、dba-note、k8s-note、linux、python/7drf-vue、python/9嘤鸣APP、python/8路飞2.0
  - `_navbar.md` ×4: k8s-note、k8s-2023、dba-note、linux
  - `.nojekyll` ×2: linux、k8s-note
  - 无 `_coverpage.md`/`_404.md`, 也无含 `$docsify` 的 index.html 入口。这些 `_*.md` 已被 mkdocs.yml 的 exclude_docs(`**/_*.md`)排除, 不生成页面; `.nojekyll` 会被复制进 site/(无害)。
- 已删除上述 14 个 docsify 残留文件(`_sidebar.md`×8、`_navbar.md`×4、`.nojekyll`×2), 用 find 在内容目录按文件名匹配删除; 仓库现无 docsify 遗留文件, 仅 mkdocs 工程。
- 任务5(清理临时脚本 + 记录使用说明): 盘点仓库根/mkdocs-docs/ 无 *.sh/*.cmd/*.bat 临时脚本; 内容目录 .py 均为笔记示例(保留); generate_nav.py、mkdocs.cmd/.sh 此前已删, 无新临时脚本可清。仓库根 `README.md` 已有完整 mkdocs 使用说明, 本次在其末尾追加「四、工程清理与忽略规则」(site/ 不进 git、.mkdocsignore 作用、nav 手工维护、docsify 弃用、.DS_Store 清理), 落实"记录使用说明到 README"。
- 主题定制(用户要求): (1) `mkdocs.yml` theme 加 `repo_url: https://github.com/chengkanghua/docs` 与 `repo_name`, 顶部导航栏右侧显示 GitHub 图标链接; (2) 顶部 header+tabs 改浅灰——发现 `extra.css` 此前实际缺失(被 extra_css 引用但文件不在, 致 README 所述窄屏横排样式一直未生效, 且 list_dir 曾误显示其存在), 已创建 `mkdocs-docs/mkdocs-docs/extra.css`; 用户随后手动改为中灰(#6a6a6a)。(3) 用户要渐变导航栏, 已改为蓝青渐变 `linear-gradient(135deg, #1976d2, #0097a7)` + 白字(清晰好看), 窄屏横排保留。
- 主题微调(用户反馈): 顶部导航字号之前设 .95rem+600 字重 + 窄屏 `.md-tabs` 横排 `overflow-x:auto`(导航项多+字号放大后右侧被裁, 导致"只显示一半"); 已撤掉该媒体查询, 字号恢复 material 默认(.7rem)再放大 20%→.84rem、字重 500, hover 仅留轻微高亮(rgba(255,255,255,.14)), 渐变+阴影保留。
- 用户最终决定: 顶部菜单栏样式全部恢复 material 默认——已删除 `mkdocs-docs/mkdocs-docs/extra.css` 并从 `mkdocs.yml` 移除 `extra_css` 引用。GitHub 图标(repo_url 在顶层)保留, 用户确认已正常显示。结论: 不要再给顶部导航栏加任何自定义 CSS。
