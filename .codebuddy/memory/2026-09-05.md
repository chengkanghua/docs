# 2026-09-05

- 扩充仓库根 `.gitignore`(用户要求): 加入 Python 虚拟环境(venv/env/.venv/ENV)、编译缓存(*.py[cod]/.Python)、打包构建(build/dist/*.egg-info)、测试/lint缓存(.pytest_cache/.mypy_cache/.ruff_cache/.tox/.hypothesis/.coverage/htmlcov)、IDE(.idea/.vscode)、mkdocs 缓存(.cache/) 等全量 Python 忽略项, 保留 site/ 忽略。
- 删除仓库全部 `.DS_Store`(macOS 系统文件) 共 7 个, 位于 mkdocs-docs/mkdocs-docs/ 下笔记目录(k8s-note 的 .assets 与 chart包/、linux 多个 .assets、python/1Python基础V2.0/)。.gitignore 已含 .DS_Store 忽略, 后续不会重新提交。
- 环境提示: `execute_command` 的 shell 是 **Git Bash**(Windows), 需用 Unix 命令(如 find/rm); 不要嵌套 `powershell -Command "..."` 且内部用 `$变量`(bash 会把 `$` 当变量展开导致空)。这也解释了之前 `.DS_Store`/PowerShell 命令失败的原因。
- 创建 `mkdocs-docs/.mkdocsignore`(与 mkdocs.yml 同级, 用户要求"mkdocs 的忽略文件也添加上"): 内容与 .gitignore 对齐, 忽略 .DS_Store、Python 缓存/虚拟环境、IDE 配置、第三方代码噪音(uric_web/myblog/bootstrap/VueDemo/Muban/statics, 与 mkdocs.yml 的 exclude_docs 双保险), 防止这些文件被复制进 site/。mkdocs.yml 的 `exclude_docs` 已覆盖 `**/_*.md`/`**/*.pyc`/`**/__pycache__/` 及上述代码目录。
- 确认部署方式(用户澄清): `site/` 构建产物不进 git(已被根 `.gitignore` 忽略 `site/`), 用 `mkdocs gh-deploy` 推送到 `gh-pages` 分支; `.mkdocsignore` 与 git 管理无关, 仅让部署产物更干净(不复制 .DS_Store/venv/缓存等进 site/)。运行位置需在 `mkdocs-docs/`(因 mkdocs.yml 在该子目录)。
