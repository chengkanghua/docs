# 2026-09-04 工作记录

## 任务: 将 docsify 站点改造为 mkdocs-material

用户仓库 c:\Users\kanghua\docs(文档根), 原为 docsify(各目录含 _sidebar.md/_navbar.md)。

### 结论(已与用户确认)
- 用户选择: 顶部菜单依据根 `_navbar.md`, 每栏目侧边菜单用**各栏目自带的 `_sidebar.md`**(python 用 python/_sidebar.md、auto 用 auto/_sidebar.md、k8s 用 k8s-note 与 k8s-2023 各自的、linux 用 linux/_sidebar.md、dba 用 dba-note/_sidebar.md)。
- 目录里存在但 _sidebar 未收录的 .md 自动补全进导航, 保证内容不丢。
- mkdocs 1.6 限制: docs_dir 不能 == 配置文件目录, site 不能在 docs_dir 内 ⇒ “mkdocs.yml 在仓库根 + docs_dir=根 + site/在仓库内”不可行。用户选定布局: **mkdocs 工程放与 docs 平级的 c:\Users\kanghua\mkdocs-docs\**, docs_dir=../docs(内容原地复用不复制), site/ 输出到该工程目录, 仓库根放一键脚本。

### 落地成果
- `c:\Users\kanghua\mkdocs-docs\`: `mkdocs.yml`(material 主题, navigation.tabs+sticky, use_directory_urls=false, 中文搜索 separator), `generate_nav.py`(解析 _navbar/_sidebar 生成 nav, 嵌套 _sidebar 如 python/9嘤鸣APP 自动采用), `site/`(成品 ~1.8GB, 因内容目录含大量图片/html/py 原样复制)。
- 仓库根新增 `mkdocs.cmd` 与 `mkdocs.sh`(用法: build 或 serve, serve 端口 127.0.0.1:8090; 每次先跑 generate_nav.py 再 build)。
- venv 在 仓库根 `.venv`(装好 mkdocs + mkdocs-material + pymdown-extensions)。
- 导航共 223 页。自动补全: auto +22、k8s-note +1。
- 已删除根目录临时产物 `mkdocs.yml`、`_mkdocs_nav_gen.py`(避免被 mkdocs 复制进输出)。

### 已知事项 / 备忘
- _sidebar 引用了 3 个不存在的文件, 生成时自动跳过:
  - auto/day03-数据类型下/第一阶段考试题 (auto/copy).md(实际文件无 "auto/" 段)
  - python/2函数和模块v2.0/README.md、python/3面向对象编程v2.0/README.md(目录里只有笔误的 REDME.md)
- md 内容里原有若干"相对路径图片/文件"链接指向错误目录(如 assets/ 应同级但缺), mkdocs 构建会报 WARNING, 属内容历史问题, 非配置问题。
- material 顶部会打印 MkDocs 2.0 迁移公告, 不影响使用。
- 本 IDE 环境中批量删除大量文件(如清空 site 7000+ 文件)会触发 safe-delete 确认, 手动先删 site 再 build 即可。
- 本地 git 仓库: 未提交任何更改(README.md 是既有改动, mkdocs.cmd/mkdocs.sh 新文件未跟踪)。

## 任务5: 清理 + 使用说明(同日续)
- 清理: 删除根目录历史遗留 `index.html.bak`(docsify 首页旧备份)、`nohup.out`(无效 docsify serve 日志)。根目录现只剩 docsify 必需文件 + mkdocs.cmd/.sh + README.md, 无临时脚本。
- 修复泄漏: docs_dir=../docs 会把根目录的 `mkdocs.cmd`/`mkdocs.sh` 原样复制进 site 成品。已在 `generate_nav.py` 的 exclude_docs 增加 `/mkdocs.cmd`、`/mkdocs.sh`(该脚本是 mkdocs.yml 的权威模板, 改动须改这里而非直接改 yml)。已清空 site 全量重建验证: site 顶层仅 404.html/CNAME/index.html/sitemap 等正常产物。
- README.md 末尾新增 “## MkDocs 构建说明”章节(工程布局/一键命令/环境依赖/日常更新), 该 README 同时是 docsify/mkdocs 站点 En 首页。

## 用户后续要求: 工程入仓 + 去掉脚本(同日)
- 用户要求: 工程目录搬进 docs 仓库内, 删掉根目录一键脚本, 进入目录直接 `mkdocs serve`。已照做:
  - 工程迁至 `docs/mkdocs-docs/`(含 mkdocs.yml、generate_nav.py), 平级的 `c:\Users\kanghua\mkdocs-docs` 已删除; 根目录 mkdocs.cmd/mkdocs.sh 已删。
  - generate_nav.py 路径改为: PROJ=DOCS/"mkdocs-docs", docs_dir="..", site_dir="../../mkdocs-site", exclude_docs 新增 `/mkdocs-docs/`。
- 实测结论(mkdocs 1.6.1): **docs_dir 可以指向配置文件所在目录的父目录**(允许); 但 **site_dir 绝不能位于 docs_dir 内** ⇒ 成品改为输出到仓库外 `c:\Users\kanghua\mkdocs-site`(顺带避免 1.8G 进 git)。
- 全局 Python(Python312)原本只有 mkdocs 缺 material, 已补装 mkdocs-material 9.7.7(pymdown-extensions 11.0.2 已有) ⇒ 进目录可直接 `mkdocs serve`(默认 http://127.0.0.1:8000)。仓库根 .venv 保留未动(备用), 已不再需要。
- 验证: `mkdocs build` 44 秒成功(223 页, 成品 1.8G, 无工程文件泄漏), `mkdocs serve` 已实测可访问。
- 本环境坑: 长命令(含 sleep)的 stdout 常被吞; 后台跑任务 + 读日志文件更可靠。

## 用户再次重构为官方布局(终版)
- 用户把内容目录整体移入工程: 仓库根仅剩 README.md/CNAME/requirement.txt/mkdocs-docs/; 工程内 `mkdocs-docs/`(内容) + `site/`(成品) + `mkdocs.yml`。即官方: 工程根含 mkdocs.yml 与内容子目录。
- 调整: mkdocs.yml 改 `docs_dir: mkdocs-docs`、`site_dir: site`、exclude_docs 精简为 `**/_*.md / **/*.pyc / **/__pycache__/`; 顶部注释改为导航手工维护说明。
- 把仓库根 README.md(首页)与 CNAME 复制到内容目录根(成品需含)。
- 删除 generate_nav.py(用户要求; 它只是按 docsify _sidebar.md 自动生成 nav 的一次性脚本, nav 已固化)。
- 旧预览进程(taskkill)后重新构建: 45.30s 成功, 成品 1.8G(273 个 sitemap URL), 各栏目首页链接均在。
- `.gitignore` 追加 `/mkdocs-docs/site/`(否则 1.8G 成品进 git); 实测 `git status --ignored` 显示 site 已被忽略。
- 提示用户: 旧成品 `c:\Users\kanghua\mkdocs-site`(1.8G)可删; `.nojekyll` 被用户删除(D .nojekyll), 若部署 GitHub Pages 需补回(放内容目录)。
- `.gitignore` 整理: 合并两处 mkdocs 区块, 删无效根级 `/site`(旧布局残留), 加 `.cache/`(mkdocs 缓存); 保留 `/mkdocs-docs/site/` 忽略并验证生效。
- mkdocs.yml 的 exclude_docs 追加第三方代码库目录(uri_web/myblog/bootstrap/VueDemo/Muban/statics), 消除 `mkdocs serve` 时的噪音 INFO(那些是框架自带 README/CHANGELOG, 非笔记)。其余 INFO(用户自己的笔记 md 不在 nav)与坏链接 WARNING 属内容问题, 未动。
- 导航一致性检查: nav 引用 246 个 md, 实际目录(去噪音)251 个, **坏链 0** → 当前 yml 的 nav 与目录完全对齐; 顶层标签 En/python/auto/k8s-devops/dba/linux 即"跟以前一样"的顶部导航(源于旧 _navbar.md, 已固化)。仅 5 个无关文件未收录(2 笔误 REDME、2 代码子目录 README、1 个 python/5前端基础/web开发基础v1.0.md)。用户重构时删了 _navbar.md/_sidebar.md, 导航靠 yml 固化存活。
- 待用户澄清"顶部导航不符"的具体点(可能他期望 mkdocs 左侧像 docsify 那样"只显当前栏目目录", 而 mkdocs material 默认全站侧边栏)。
- 按用户选择(选项1+3): mkdocs.yml 的 nav 把 `k8s-devops` 拆成两个独立顶层标签 `k8s-2022`(→k8s-note/) 与 `k8s-2023`(→k8s-2023/); 现顶层 7 个标签 En/python/auto/k8s-2022/k8s-2023/dba/linux。tabs 渲染机制已确认(site/index.html 含 md-tabs, navigation.tabs 配置本就正确); 之前"顶部标签没出现"多为浏览器缓存或窄屏折叠成汉堡菜单。
- 用户进一步澄清: 他要的是导航栏顶部的水平标签; 实际是 material 响应式断点——窗口 <~1220px(76.25em) 时顶部 tabs 被收进左上角汉堡菜单(点开左侧抽屉)。已新建 `mkdocs-docs/mkdocs-docs/extra.css`(yml 中 `extra_css: [extra.css]` 挂接), 用 `@media (max-width:76.24em)` 把显示断点降到更窄并允许 `.md-tabs__list` 横向滚动, 让窄屏/分屏也直接在顶部显示标签栏。
- mkdocs serve 警告排查与处理: ① MkDocs 2.0 横幅=信息性忽略; ② v12 冲突(README.md 与 index.html)=mkdocs 已自动 Excluding, 代码目录被误扫, 可忽略; ③ 4 个文件不在 nav 警告 → 已加进 nav(python/5前端基础/web开发基础v1.0.md, python/2、3函数模块 REDME.md, auto/.../day04模块相关/代码/xiejun/README.md); ④ 根 README.md `[Get Started](/)` 绝对链接 → 改为 `python/README.md`; ⑤ 图片/链接缺失(assets/*.png、harbor docs/*.md 等)经全盘搜索确认文件不在仓库, 属源内容缺失, 需用户补图才能消除, 否则仅破图无害。
- 用户确认后, 把两个拼写错误的源文件重命名为规范名: `python/2函数和模块v2.0/REDME.md`→`README.md`, `python/3面向对象编程v2.0/REDME.md`→`README.md`; 并同步更新 mkdocs.yml 中两处 nav 路径(条目标题仍叫 REDME, 仅路径改 README.md)。
- 图片缺失二次排查(用户选"检查目录实际内容"): 各目录 assets 实际有大量图(day07=116, day04=394, day05=417, 6Django assets=26/assets2.0=59), 但报错的那几个特定文件确属未带入(搜近似名均 0): day07 缺 hash 名 `1235389-*.png`; day04/day05 缺 `截屏2022-06-21 20.31.34.png`; 6Django Django2.0 引的 `logo.gif`/`copycode-*.gif` 在 assets2.0 里只有 png/jpg 故缺失; django3.2 引的 `屏幕快照 2021-10-31*.png` 缺失。harbor README 引的 docs/High Availability.md 等 3 个 .md 因无 docs/ 子目录失效(属 chart 仓库文档, 非图片, 与笔记无关, 可直接注释链接消除警告)。结论: 图片需用户补原图; harbor 3 链接可注释。
- 精简仓库根 `requirement.txt`(原 29 包, pip freeze 产物; 注意用户最初误把笔记示例里的 requirements.txt 当成项目文件, 实际在仓库根且文件名为单数 requirement.txt): 对照 mkdocs.yml(仅用 mkdocs+mkdocs-material, plugins 仅 search) 判断, mkdocs-material 依赖树外的 requests 网络链 6 包(requests/certifi/charset-normalizer/idna/urllib3/six)属环境杂物, 已 `pip uninstall -y` 成功; requirement.txt 删此 6 行剩 23 行。其余 23 包(mkdocs/mkdocs-material/pymdown-extensions/Markdown/babel/PyYAML/Jinja2/Pygments/watchdog 等)均为 material 运行必需依赖, 不可删。验证 `mkdocs --version` 正常。
- 用户要求给仓库根 `README.md`(此前为空) 写项目介绍。已写: 项目结构(仓库根关键文件 + 内容目录 mkdocs-docs/mkdocs-docs 下各栏目 python/auto/k8s-note/k8s-2023/dba-note/linux 等)、如何更新(改 .md / 在 mkdocs.yml 的 nav 加条目 / 图片放 assets / `cd mkdocs-docs && mkdocs serve` 预览 / 发布)、常见问题。关键结构核实: mkdocs 配置在 `mkdocs-docs/mkdocs.yml`, docs_dir=`mkdocs-docs/mkdocs-docs`, site_dir=`site`(即 mkdocs-docs/site); 站点首页 README 是 mkdocs-docs/mkdocs-docs/README.md(En 标签), 与仓库根 README 不同(仓库根 README 是 GitHub 项目说明)。
- 发布方式: 用户用 `mkdocs gh-deploy` 发布到 GitHub Pages(自动 build 并推 gh-pages 分支), 非手动 mkdocs build。自定义域名由内容目录 `mkdocs-docs/mkdocs-docs/CNAME` 控制, 发布后自动生效。已将 README 发布小节改为 gh-deploy 写法。
- 任务5(清理临时脚本+记录使用说明): 盘点仓库根与 mkdocs-docs/ 工程层, 未发现遗留临时脚本(generate_nav.py、mkdocs.cmd/sh 启动脚本、*.log 均不存在, 此前会话已清理); 笔记内容目录里的 .py/.sh 为课程示例代码未动。使用说明已落根 README.md(项目结构/如何更新/mkdocs gh-deploy 发布/常见问题) 并记于本日记忆。site/ 为 mkdocs 构建产物(约1万文件), 非脚本; 已新增仓库根 `.gitignore` 忽略 `site/`(及 __pycache__/*.pyc/*.log 等), 避免将构建产物提交进仓库。结论: 临时脚本无遗留, 任务5 完成。
- 扩充仓库根 `.gitignore`(用户要求): 加入 Python 虚拟环境(venv/env/.venv/ENV)、编译缓存(*.py[cod]/.Python)、打包构建(build/dist/*.egg-info)、测试/lint缓存(.pytest_cache/.mypy_cache/.ruff_cache/.tox/.hypothesis/.coverage/htmlcov)、IDE(.idea/.vscode)、mkdocs 缓存(.cache/) 等全量 Python 忽略项, 保留 site/ 忽略。
